
spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:db
    username: sa
    password:

  jpa:
    properties:
      hibernate:
        format_sql: true
        show_sql: true
    hibernate:
      ddl-auto: create
  config:
    import: classpath:gitlab-secret-key.yml


jenkins:
  baseurl: http://211.205.161.133:7474/


